<h3> Sign in </h3>

<%= form_for :user do |f| %>
	<%= f.label :email %> 
	<%= f.text_field :email %>
	<%= f.label :password %> 
	<%= f.password_field :password %>

	<%= f.submit 'Log in' %>
<% end %>

</br><%= link_to("Sign up", new_user_path) %>