<h2>Edit product</h2>

<%= form_for @product do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.label :photo %>
	<%= f.text_field :photo %>
	<%= f.label :price %>
	<%= f.text_field :price %>

	<%= f.submit %>
<% end %>

 